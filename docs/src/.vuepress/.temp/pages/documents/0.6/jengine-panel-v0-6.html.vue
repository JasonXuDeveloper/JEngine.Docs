<template><p>{% import &quot;views/_data.njk&quot; as data %}</p>
<h1 id="jengine面板-v0-6" tabindex="-1"><a class="header-anchor" href="#jengine面板-v0-6" aria-hidden="true">#</a> JEngine面板（v0.6）</h1>
<p>JEngine为Unity开发设计的面板</p>
<h2 id="功能" tabindex="-1"><a class="header-anchor" href="#功能" aria-hidden="true">#</a> 功能</h2>
<p>JEngine面板支持双语且包含实用功能</p>
<h3 id="启动场景" tabindex="-1"><a class="header-anchor" href="#启动场景" aria-hidden="true">#</a> 启动场景</h3>
<p>Unity编辑器运行游戏后，会自动跳转至启动场景</p>
<h3 id="运行后跳转启动场景" tabindex="-1"><a class="header-anchor" href="#运行后跳转启动场景" aria-hidden="true">#</a> 运行后跳转启动场景</h3>
<p>默认勾选，勾选后，启动场景才会奏效</p>
<h3 id="上次处理热更dll实际" tabindex="-1"><a class="header-anchor" href="#上次处理热更dll实际" aria-hidden="true">#</a> 上次处理热更DLL实际</h3>
<p>该栏只读，为上次热更DLL编译时间</p>
<h3 id="热更场景快捷操作" tabindex="-1"><a class="header-anchor" href="#热更场景快捷操作" aria-hidden="true">#</a> 热更场景快捷操作</h3>
<p>此处可以快捷处理热更场景，可以进行筛选</p>
<h4 id="打开" tabindex="-1"><a class="header-anchor" href="#打开" aria-hidden="true">#</a> 打开</h4>
<p>可以打开选中的热更场景</p>
<h4 id="加载" tabindex="-1"><a class="header-anchor" href="#加载" aria-hidden="true">#</a> 加载</h4>
<p>在当前场景中，加载选中的热更场景（Additive模式）</p>
<h4 id="卸载" tabindex="-1"><a class="header-anchor" href="#卸载" aria-hidden="true">#</a> 卸载</h4>
<p>若当前场景中包含选中的热更场景，点击后会卸载（卸载Additive添加的场景）</p>
<h3 id="classbind助手" tabindex="-1"><a class="header-anchor" href="#classbind助手" aria-hidden="true">#</a> ClassBind助手</h3>
<p>批量处理全部ClassBind</p>
<h4 id="自动获取全部field" tabindex="-1"><a class="header-anchor" href="#自动获取全部field" aria-hidden="true">#</a> 自动获取全部field</h4>
<p>自动获取场景内全部ClassBind对应的类型的field</p>
<h4 id="自动获取全部fieldtype" tabindex="-1"><a class="header-anchor" href="#自动获取全部fieldtype" aria-hidden="true">#</a> 自动获取全部fieldType</h4>
<p>自动获取场景内全部ClassBind，对其Field自动分析Type</p>
<h3 id="错误修复工具" tabindex="-1"><a class="header-anchor" href="#错误修复工具" aria-hidden="true">#</a> 错误修复工具</h3>
<h4 id="程序集不存在错误" tabindex="-1"><a class="header-anchor" href="#程序集不存在错误" aria-hidden="true">#</a> 程序集不存在错误</h4>
<p>比如出现类似<code>Cannot find Type or Namespace of ILRuntime</code>的错误，点击按钮可一键修复</p>
<h2 id="面板截图" tabindex="-1"><a class="header-anchor" href="#面板截图" aria-hidden="true">#</a> 面板截图</h2>
<img src="https://s3.ax1x.com/2020/12/11/rkNP1O.png" alt="rkNP1O.png" border="0" style="width: 50%;margin-left: 25%">
<blockquote>
<p>恭喜，JEngine框架核心篇已完成！</p>
</blockquote>
</template>
